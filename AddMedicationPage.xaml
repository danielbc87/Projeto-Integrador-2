<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Health_Organizer.AddMedicationPage"
             Title="Adicionar Medicamento">

    <VerticalStackLayout Padding="20" Spacing="15">

        <!-- Banner Superior -->
        <Frame BackgroundColor="Purple" Padding="10">
            <Label Text="Adicionar Medicamento"
                   TextColor="White"
                   FontSize="18"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>
        </Frame>

        <!-- Campo Nome do Medicamento -->
        <Entry x:Name="medNameEntry" Placeholder="Nome do Medicamento"
               BackgroundColor="White"
               TextColor="Black"
               HeightRequest="50"
               Margin="0,5,0,5"/>

        <!-- Campo Horário -->
        <Entry x:Name="medTimeEntry" Placeholder="Horário (HH:MM)" Keyboard="Numeric"
                BackgroundColor="White"
               TextColor="Black"
               HeightRequest="50"
               Margin="0,5,0,5"/>

        <!-- Botão Salvar -->
        <Button Text="Salvar"
                BackgroundColor="Green"
                TextColor="White"
                FontAttributes="Bold"
                HeightRequest="50"
                Clicked="OnSaveClicked"/>

    </VerticalStackLayout>
</ContentPage>
